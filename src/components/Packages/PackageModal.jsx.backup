import React, { useState, useEffect } from 'react';
import './PackageModal.css';

const PackageModal = ({ isOpen, onClose, package: pkg, onSave, isLoading }) => {
  const [formData, setFormData] = useState({
    name: '',
    description: '',
    sessions: '2',
    classes: '8',
    price: '',
    duration: '1',
    features: '',
    isActive: true
  });

  useEffect(() => {
    if (pkg) {
      setFormData({
        name: pkg.name || '',
        description: pkg.description || '',
        sessions: pkg.sessions?.toString() || '2',
        classes: pkg.classes?.toString() || '8',
        price: pkg.price?.toString() || '',
        duration: pkg.duration?.toString() || '1',
        features: pkg.features?.join('\n') || '',
        isActive: pkg.isActive !== undefined ? pkg.isActive : true
      });
    } else {
      setFormData({
        name: '',
        description: '',
        sessions: '2',
        classes: '8',
        price: '',
        duration: '1',
        features: '',
        isActive: true
      });
    }
  }, [pkg, isOpen]);

  const handleSubmit = (e) => {
    e.preventDefault();
    
    if (!formData.name || !formData.price) {
      alert('Lütfen paket adı ve fiyat bilgilerini doldurun');
      return;
    }

    const packageData = {
      name: formData.name,
      description: formData.description,
      sessions: parseInt(formData.sessions),
      classes: parseInt(formData.classes),
      price: parseFloat(formData.price),
      duration: parseInt(formData.duration),
      features: formData.features ? formData.features.split('\n').filter(f => f.trim()) : [],
      isActive: formData.isActive,
      updatedAt: new Date().toISOString()
    };

    if (!pkg) {
      packageData.createdAt = new Date().toISOString();
    }

    onSave(packageData);
  };

  if (!isOpen) return null;

  return (
    <div className="modal-overlay" onClick={onClose}>
      <div className="package-modal" onClick={(e) => e.stopPropagation()}>
        <div className="modal-header">
          <h2>{pkg ? 'Paketi Düzenle' : 'Yeni Paket Oluştur'}</h2>
          <button className="close-btn" onClick={onClose}>×</button>
        </div>

        <form onSubmit={handleSubmit}>
          <div className="modal-content">
            <div className="form-section">
              <h4>Temel Bilgiler</h4>
              
              <div className="form-group">
                <label>Paket Adı *</label>
                <input
                  type="text"
                  value={formData.name}
                  onChange={(e) => setFormData(prev => ({ ...prev, name: e.target.value }))}
                  placeholder="Örn: Başlangıç Paketi"
                  required
                />
              </div>

              <div className="form-group">
                <label>Açıklama</label>
                <textarea
                  value={formData.description}
                  onChange={(e) => setFormData(prev => ({ ...prev, description: e.target.value }))}
                  placeholder="Paket hakkında kısa açıklama"
                  rows="3"
                />
              </div>
            </div>

            <div className="form-section">
              <h4>Haftalık Seans Sayısı</h4>
              <div className="package-types">
                <label className={`package-type-option ${formData.sessions === '1' ? 'selected' : ''}`}>
                  <input
                    type="radio"
                    name="sessions"
                    value="1"
                    checked={formData.sessions === '1'}
                    onChange={(e) => handleSessionsChange(e.target.value)}
                  />
                  <div className="type-content">
                    <span className="type-icon">⚡</span>
                    <span className="type-name">1 Seans/Hafta</span>
                  </div>
                </label>

                <label className={`package-type-option ${formData.sessions === '2' ? 'selected' : ''}`}>
                  <input
                    type="radio"
                    name="sessions"
                    value="2"
                    checked={formData.sessions === '2'}
                    onChange={(e) => handleSessionsChange(e.target.value)}
                  />
                  <div className="type-content">
                    <span className="type-icon">�</span>
                    <span className="type-name">2 Seans/Hafta</span>
                  </div>
                </label>

                <label className={`package-type-option ${formData.sessions === '3' ? 'selected' : ''}`}>
                  <input
                    type="radio"
                    name="sessions"
                    value="3"
                    checked={formData.sessions === '3'}
                    onChange={(e) => handleSessionsChange(e.target.value)}
                  />
                  <div className="type-content">
                    <span className="type-icon">🥈</span>
                    <span className="type-name">3 Seans/Hafta</span>
                  </div>
                </label>

                <label className={`package-type-option ${formData.sessions === '4' ? 'selected' : ''}`}>
                  <input
                    type="radio"
                    name="sessions"
                    value="4"
                    checked={formData.sessions === '4'}
                    onChange={(e) => handleSessionsChange(e.target.value)}
                  />
                  <div className="type-content">
                    <span className="type-icon">�</span>
                    <span className="type-name">4 Seans/Hafta</span>
                  </div>
                </label>

                <label className={`package-type-option ${formData.sessions === '5' ? 'selected' : ''}`}>
                  <input
                    type="radio"
                    name="sessions"
                    value="5"
                    checked={formData.sessions === '5'}
                    onChange={(e) => handleSessionsChange(e.target.value)}
                  />
                  <div className="type-content">
                    <span className="type-icon">🥇</span>
                    <span className="type-name">5+ Seans/Hafta</span>
                  </div>
                </label>
              </div>
            </div>

            <div className="form-section">
              <h4>Paket Detayları</h4>
              
              <div className="form-row">
                <div className="form-group">
                  <label>Ders Hakkı *</label>
                  <input
                    type="number"
                    value={formData.classes}
                    onChange={(e) => setFormData(prev => ({ ...prev, classes: e.target.value }))}
                    placeholder="8"
                    min="1"
                    max="999"
                    required
                    disabled={formData.sessions === '5'}
                  />
                  <span className="input-hint">999 = Sınırsız</span>
                </div>

                <div className="form-group">
                  <label>Fiyat (₺) *</label>
                  <input
                    type="number"
                    value={formData.price}
                    onChange={(e) => setFormData(prev => ({ ...prev, price: e.target.value }))}
                    placeholder="500"
                    min="0"
                    step="0.01"
                    required
                  />
                </div>
              </div>

            <div className="form-section">
              <h4>Özellikler</h4>
              <div className="form-group">
                <label>Paket Özellikleri (Her satıra bir özellik)</label>
                <textarea
                  value={formData.features}
                  onChange={(e) => setFormData(prev => ({ ...prev, features: e.target.value }))}
                  placeholder="Örn:&#10;Kişisel eğitmen desteği&#10;Beslenme programı&#10;Özel soyunma dolabı"
                  rows="5"
                />
              </div>
            </div>

            <div className="form-section">
              <div className="form-group checkbox-group">
                <label className="checkbox-label">
                  <input
                    type="checkbox"
                    checked={formData.isActive}
                    onChange={(e) => setFormData(prev => ({ ...prev, isActive: e.target.checked }))}
                  />
                  <span>Paketi aktif olarak işaretle</span>
                </label>
              </div>
            </div>
          </div>

          <div className="modal-actions">
            <button 
              type="button" 
              className="cancel-btn" 
              onClick={onClose}
              disabled={isLoading}
            >
              İptal
            </button>
            <button 
              type="submit" 
              className="save-btn"
              disabled={isLoading}
            >
              {isLoading ? 'Kaydediliyor...' : pkg ? 'Güncelle' : 'Oluştur'}
            </button>
          </div>
        </form>
      </div>
    </div>
  );
};

export default PackageModal;
